# REVIEW_BACKLOG.md

## 使い方
自律レビューの巡回対象。1周につき1画面・1テーマを扱う。レビュー終了時に最終レビュー日、直近の指摘、状態を更新する。

状態は `要レビュー` / `保留` / `完了` のいずれか。

| 対象画面 | 最終レビュー日 | 直近の指摘 | 状態 |
|---|---|---|---|
| ホーム | 2026-08-09 | ユーザーフィードバック6件を5PR(#101-#105)で反映。未登録をリマインダー画面へ移動、並び順を意味的優先度で固定(#98撤回)、期限切れ誤分類バグ修正、一括コピーを見出しへ、外枠なし・地色に統一(Color=1解消)。**11次元すべて2点以上、0点なし。**残りはAccessibility=2(トークン判断待ち)のみ | 完了 |
| 市場・銘柄分析 | 未レビュー | なし | 要レビュー |
| 銘柄詳細 | 未レビュー | なし | 要レビュー |
| 取り込み（会話を取り込む） | 2026-08-11 | ユーザーが自ら手動チェックし、Claudeの4パターン方式では見落としていた7件(重複告知・コピーボタン不統一・メモ候補の階層欠如・銘柄詳細との不統一・トグル位置・チップの集計対象不明瞭)を修正。手順書に「機能グループの網羅」を追加。**12次元すべて2点以上、0点・1点なし。**残りはColor=2・Accessibility=2(いずれも既存、詳細は下記)のみ | 完了 |
| リマインダー一覧 | 未レビュー | なし | 要レビュー |
| リマインダー詳細 | 未レビュー | なし | 要レビュー |
| 設定 | 未レビュー | なし | 要レビュー |
| 注目ポイント編集シート | 未レビュー | なし | 要レビュー |
| 指標編集シート | 未レビュー | なし | 要レビュー |
| 業績推移編集シート | 未レビュー | なし | 要レビュー |

## ホームの未解決事項(2026-08-09)

11次元採点の詳細は `DECISIONS.md` の同日エントリを参照。

### 解消済み
1. ~~実運用摩擦=0 / 期限切れからリマインダー本体へ行けない~~ → #96(0→3)
2. ~~一貫性=1 / 空表現とゼロ状態~~ → #97(1→3)
3. ~~認知負荷=1 / 静かな日の画面上部~~ → #98(1→3)、のちユーザー指摘で並び順を意味的優先度に修正 → #102
4. ~~情報アーキテクチャ: リマインド未登録がホームに常駐~~ → #101 でリマインダー画面へ移動(ユーザー指示)
5. ~~バグ: 今日登録したリマインダーが期限切れに分類される~~ → #103
6. ~~一括コピーの位置~~ → #104(見出しの文字ボタンへ、ユーザー指示)
7. ~~Color=1 / 赤トークンの意味の衝突~~ → #105 で外枠なし・地色に統一し解消(ユーザー指示)
8. Accessibility=1 → #99 で部分解消(1→2)。案内文とボタンのラベルはAA達成。残りは下記。

### 残っているもの(トークン定義の判断が要る)

**Accessibility=2 / 補助情報のコントラスト不足**
`--ink3`(#8b95a1)はどの地色でもAAに届かない(白3.04 / app2.86)。
`.hint` と `.btn.ghost` は `--ink2` にしたが、次が残っている。

- `.list-row .meta`(11px・白地3.04:1) … 銘柄コード、件数、次回日時など。全画面に多数
- `.muted`(12px・app地2.86:1) … 空区分の「なし」
- `.empty` の案内文(16px・app地2.86:1) … ゼロ状態の説明。ホーム含む4画面で共通

**なぜ自律で直さないか:** `--ink3` を色味を保ったままAAに届かせると `--ink2`(#5a6672)より濃くなり、
文字色の3段階(ink / ink2 / ink3)が実質2段階に潰れる。これはトークン定義の変更にあたる。
選択肢は「濃くして階層を諦める」「該当箇所を個別に `--ink2` へ寄せる」「文字サイズを上げて大文字扱い(3:1)にする」「現状を許容すると明示的に決める」。

## 取り込みの未解決事項(2026-08-10)

11次元採点の詳細は `DECISIONS.md` の同日エントリを参照。

### 解消済み
1. ~~認知負荷=1 / 一貫性=1 / 「①説明書をAIに渡す」が完了状態を記憶せず毎回②と同じ重さで最上部を占有~~ → ①を「困ったときは」と同じ開閉パターンに揃え、説明書コピー後は次回訪問時から折りたたむ(情報は削除せず、いつでも再展開できる)
2. ~~ユーザー指摘5件(似た指標ボックスの表示・該当セクション不明・メモ候補UI)~~ → PR #116で反映済み
3. ~~リマインダー候補の本文が文字数切り詰めで、タップ領域も行全体だった~~ → 3行クランプ+タップで全文表示に変更、トグルのタップ領域を分離(2026-08-11)
4. ~~メモ候補にアイコンが無く、リマインダーと選び方(スワイプ削除 vs トグル)も違った~~ → `memoIcon()`追加、トグル方式に統一、「＋追加」は削除(ユーザー指示、2026-08-11)
5. ~~3行クランプが実際には4行以上になっていた(バグ)~~ → `.tx`のflex化が`.clip3`のflexアイテム化を招いていた(flexとline-clampの既知の相性問題)ことが原因。`.tx`のflexを解除して修正(2026-08-11)
6. ~~「日付つきの注目ポイント」見出しが区分見出しの「リマインダー」と語が同期していない~~ → 「リマインダー登録」に変更、取り込み元の説明は案内文に移動(ユーザー指示、2026-08-11)
7. ~~「新しい指標名N」「読取N行」チップは判断材料にならない~~ → 削除(ユーザー指示、2026-08-11)
8. ~~凡例が表限定でない機能まで説明し、横一列で読みにくい~~ → 「セルをタップで全文表示」を削除、「削除対象」「新規追加」に簡潔化、項目ごとに改行(ユーザー指示、2026-08-11)

### 2026-08-10(2回目)の12次元(4パターン方式)Phase1-2

11次元→12次元(データ整合性を追加)化・「機械測定群/理解のしやすさ/整合性/運用コスト」の4パターンに分けて観察した詳細は `DECISIONS.md` 同日エントリを参照。

- **視覚的ノイズ=2 → 3**: 装飾要素数(枠線+地色)を実測(0→10→86、3状態)し、件数に比例していて過剰でないことを確認。前回「見送り」としていたが、実測の結果、この周で3点に格上げする。
- **一貫性**: メモ候補カードと個別銘柄`hypoMiniCard`のcomputed style(背景・枠線・padding)が完全一致することを実測で確認。問題なし。
- **実運用摩擦**: `commitImport()`が取り込み完了後に必ず`openStock()`で銘柄詳細へ遷移する挙動について、複数銘柄を連続取り込みする使い方なら画面切替の反復になり得ると考え、ユーザーに実際の使い方を確認した。回答は「1回の会話で基本1銘柄」であり、現在の遷移は結果確認として適切と判断。**変更不要。**

### 2026-08-11 ユーザーの手動チェックで見つかった7件(Claudeの4パターン方式では見落とし)

詳細・見落とし理由の分析は `DECISIONS.md` 同日エントリを参照。手順書(`UI_REVIEW_LOOP.md`)にも
「パターン②③は機能グループを網羅する」を追加済み。

1. ~~認知負荷: 「新しい指標が○件増えます」が読み込み内容確認の表と重複表示~~ → 削除(件数はチップ、名前は表で見える)
2. ~~一貫性: コピーボタン(表記をそろえる指示・整形しなおすプロンプト)にアイコンが無くHome等と不統一~~ → `copyIconSvg()`を追加
3. ~~情報構造/一貫性: メモ候補がリマインダー候補と違い枠なしのプレーンラベルで「まとめただけ」に見えた~~ → `.candbox`を共有しカード化
4. ~~一貫性: リマインダー候補の日付/本文が独自クラス(`.d`/`.b`)で個別銘柄側と文字の見せ方が違った~~ → `.ttl`/`.meta`に統一
5. ~~一貫性: ON/OFFトグルが左側で一般的な配置(右側)と逆~~ → 右側に変更
6. ~~データ整合性: 「新しい指標N」チップが似た指標の数とも表の新規数とも合わない~~ → 「新しい指標名N」に改称し集計対象を明示
7. **判断保留(中立)**: 似た指標警告(`.metwarn`)の黄色背景の必要性 → 判断が要る項目への注意喚起として不合理ではないと判断し、今回は変更しなかった。再度指摘があれば取り上げる。

### 残っているもの(この周では見送り、ホームと同種の既存課題)
- **Color=2** … `--red-bg`(`chip.red`)が「銘柄を特定できません」警告・`需給確認`カテゴリタグ・選択数超過警告・`該当セクション不明`(orphan行、今回追加)の**少なくとも4つの異なる意味**で使われていることをgrepで確認した(`--amber-bg`の「似た指標警告」/「読めなかった行」の2重複と合わせ、既存の色トークン意味重複はこの画面に複数残っている)。Homeで見つかった「同色の二重の意味」と同型の既存課題で、実害(誤操作)は未確認のため、この周も見送り。次にColorを扱う周でHomeとまとめて棚卸しする。
- **Accessibility=2** … `.meta`/`.muted` 等(`--ink3`)のコントラスト不足。ホームで確認済みの全画面共通の既知の残課題(トークン定義の判断待ち、上記「ホームの未解決事項」参照)。

## 保留課題

### 配色の見直し
`--navy:#123a5e` が重い。タブバー・バッジ・グラフ・チップに波及するため、実装前に使用箇所の洗い出しと案の提示が必要。

**状態:** 保留
**境界:** トークン変更＝ユーザー確認が必要。

### padding / margin のトークン化
現在 spacing 契約は `gap` にのみ適用されている。padding / margin はトークン外の値が正当な実装として存在する(入力欄の `padding:10px 12px` など)。統一するかどうかは未決。

**状態:** 保留
**境界:** トークン範囲の拡張＝ユーザー確認が必要。


## 完了

### 色トークン・esc/escArg・シート内タップ領域の検査(2026-08-09)
`test/e2e/colortoken.js` / `escuse.js` / `sheettap.js` を追加。無検査だった3項目を機械検査下に置いた。
シート内の実測で `.stat` バッジ36px・`select` 43pxの違反が見つかり、あわせて44pxへ修正した。
