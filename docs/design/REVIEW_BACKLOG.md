# REVIEW_BACKLOG.md

## 使い方
自律レビューの巡回対象。1周につき1画面・1テーマを扱う。レビュー終了時に最終レビュー日、直近の指摘、状態を更新する。

状態は `要レビュー` / `保留` / `完了` のいずれか。

## 機会バックログの使い方
`UI_REVIEW_LOOP.md` Phase 1.5b で見つけた「機会」候補を記録する。欠陥バックログ(上記の対象画面表)とは
別に管理する。理由は、欠陥は直せば消えるが、機会は**見送っても価値が消えるわけではなく**、
前提が変わったときに再評価すべきものだから。

- 「画面ごとの機会」は、その画面のPhase 1.5b実施時に見つけたもの。表の直近の指摘欄からもリンクする。
- 「画面横断の機会」は、通し実行の途中で他画面にも関わると分かったもの。
- 各候補は「候補 / 効果 / コスト / 代替 / 判定(採用・見送りと理由)」をセットで書く。
- 見送った候補も削除しない。前提が変わったら見直す(`DESIGN_SCORECARD.md`「対応済みの意思決定」参照)。

### 画面横断の機会
(まだ無し)

### 画面ごとの機会
- **取り込み(会話を取り込む)**: 業績推移の更新前/更新後で、変更のあった年度だけ強調する。判定: 採用候補だったが、実装着手時に前提の欠陥(部分更新で他年度のデータが消える)を発見したため保留。詳細は下記「取り込み画面: 新方式での初回レビュー(2026-08-15)」「業績推移の部分更新で、貼り付けに無い年度のデータが削除される」。

| 対象画面 | 最終レビュー日 | 直近の指摘 | 状態 |
|---|---|---|---|
| ホーム | 2026-08-09 | ユーザーフィードバック6件を5PR(#101-#105)で反映。未登録をリマインダー画面へ移動、並び順を意味的優先度で固定(#98撤回)、期限切れ誤分類バグ修正、一括コピーを見出しへ、外枠なし・地色に統一(Color=1解消)。**11次元すべて2点以上、0点なし。**残りはAccessibility=2(トークン判断待ち)のみ | 完了 |
| 市場・銘柄分析 | 未レビュー | なし | 要レビュー |
| 銘柄詳細 | 未レビュー | ユーザー報告2件をアドホックに修正(正式レビューは未実施): 「調べる項目を選ぶ」の深掘りテーマが「注目テーマ」カードと保存先を共有しバグの元だった件を分離、セクション管理への重複導線を削除。詳細は下記「調べる項目を選ぶの未解決事項」 | 要レビュー |
| 取り込み（会話を取り込む） | 2026-08-22(3周目) | 2周目レビューで新規発見した3件(確認テーブルのタップ目印欠如、`.d-del`の低コントラスト、凡例の記録漏れ)をすべて解消(ユーザー承認)。ユーザー指示で凡例を「|」区切りの1行に変更(2026-08-20(5))。さらにユーザー指摘「読めなかった行のエリアが似た名前の指標とデザインが違う」「枠線が多くてビジー感がある」を受け、カード3種(似た指標/読めなかった行/メモ候補)の外枠・行区切り線を廃止し二層背景(`.cbx-head`/`.cbx-body`)に統一(2026-08-22、ユーザー承認済みモックアップの実装)。残るは`.metwarn .d`の低コントラスト(全画面共通課題の一部、背景色変更後も未解消)と機会候補1件(似た指標→指標設定シートへの直接リンク、採用候補・未着手)。**2026-08-22に3周目レビュー(①からの通し実行)を実施し、新規に7件を発見**(確認テーブルの更新後列が読めない=情報構造1、`.swtap`/`.tx`の44px割れ=Hit Area1、データ整合性3件=リマインダー件数の不一致・禁止項目のメモ取り込み・2回目に警告が出ない、ほか一貫性/実運用摩擦)。実装は未着手。詳細は下記「取り込み画面: 3周目レビュー」。従来の指摘群は解消済みのまま | 要レビュー |
| リマインダー一覧 | 未レビュー | なし | 要レビュー |
| リマインダー詳細 | 未レビュー | なし | 要レビュー |
| 設定 | 未レビュー | なし | 要レビュー |
| 注目ポイント編集シート | 未レビュー | なし | 要レビュー |
| 指標編集シート | 未レビュー | ユーザー質問をきっかけに、指標マスターの重複(EPS↔1株当たり当期純利益、ROE↔自己資本利益率等)をアドホックに修正(正式レビューは未実施)。2026-08-15、ユーザー指示で表示On/Offをトグルスイッチ化。詳細は下記「指標マスターの表記ゆれ統合」「指標設定シートのトグル化」 | 要レビュー |
| 業績推移編集シート | 未レビュー | 同上。2026-08-15、業績推移テーブルの指標名を2行表示化。さらにグラフ選択の方式自体を、設定画面で事前選択(上限4件)する方式から、銘柄詳細の表タップで即切り替え・上限なしの方式に変更(グラフOn/Offトグルは廃止)。同日、ユーザー報告バグ2件を修正: 「区切り線が指標列だけずれる」(`display:-webkit-box`をtd直付けからspanへ)、「表が縦に長いとタップしてもグラフが画面外のままになる」(表を5行までのファーストビュー+縦スクロールエリア化)。2026-08-20、ユーザー指摘で業績推移テーブル(銘柄詳細に埋め込み・`renderTrend()`)の指標名列を横スクロール時も固定(`position:sticky; left:0`)。詳細は`DECISIONS.md`の2026-08-20(4)エントリを参照 | 要レビュー |

## ホームの未解決事項(2026-08-09)

11次元採点の詳細は `DECISIONS.md` の同日エントリを参照。

### 解消済み
1. ~~実運用摩擦=0 / 期限切れからリマインダー本体へ行けない~~ → #96(0→3)
2. ~~一貫性=1 / 空表現とゼロ状態~~ → #97(1→3)
3. ~~認知負荷=1 / 静かな日の画面上部~~ → #98(1→3)、のちユーザー指摘で並び順を意味的優先度に修正 → #102
4. ~~情報アーキテクチャ: リマインド未登録がホームに常駐~~ → #101 でリマインダー画面へ移動(ユーザー指示)
5. ~~バグ: 今日登録したリマインダーが期限切れに分類される~~ → #103
6. ~~一括コピーの位置~~ → #104(見出しの文字ボタンへ、ユーザー指示)
7. ~~Color=1 / 赤トークンの意味の衝突~~ → #105 で外枠なし・地色に統一し解消(ユーザー指示)
8. Accessibility=1 → #99 で部分解消(1→2)。案内文とボタンのラベルはAA達成。残りは下記。

### 残っているもの(トークン定義の判断が要る)

**Accessibility=2 / 補助情報のコントラスト不足**
`--ink3`(#8b95a1)はどの地色でもAAに届かない(白3.04 / app2.86)。
`.hint` と `.btn.ghost` は `--ink2` にしたが、次が残っている。

- `.list-row .meta`(11px・白地3.04:1) … 銘柄コード、件数、次回日時など。全画面に多数
- `.muted`(12px・app地2.86:1) … 空区分の「なし」
- `.empty` の案内文(16px・app地2.86:1) … ゼロ状態の説明。ホーム含む4画面で共通

**なぜ自律で直さないか:** `--ink3` を色味を保ったままAAに届かせると `--ink2`(#5a6672)より濃くなり、
文字色の3段階(ink / ink2 / ink3)が実質2段階に潰れる。これはトークン定義の変更にあたる。
選択肢は「濃くして階層を諦める」「該当箇所を個別に `--ink2` へ寄せる」「文字サイズを上げて大文字扱い(3:1)にする」「現状を許容すると明示的に決める」。

## 取り込みの未解決事項(2026-08-10)

11次元採点の詳細は `DECISIONS.md` の同日エントリを参照。

### 解消済み
1. ~~認知負荷=1 / 一貫性=1 / 「①説明書をAIに渡す」が完了状態を記憶せず毎回②と同じ重さで最上部を占有~~ → ①を「困ったときは」と同じ開閉パターンに揃え、説明書コピー後は次回訪問時から折りたたむ(情報は削除せず、いつでも再展開できる)
2. ~~ユーザー指摘5件(似た指標ボックスの表示・該当セクション不明・メモ候補UI)~~ → PR #116で反映済み
3. ~~リマインダー候補の本文が文字数切り詰めで、タップ領域も行全体だった~~ → 3行クランプ+タップで全文表示に変更、トグルのタップ領域を分離(2026-08-11)
4. ~~メモ候補にアイコンが無く、リマインダーと選び方(スワイプ削除 vs トグル)も違った~~ → `memoIcon()`追加、トグル方式に統一、「＋追加」は削除(ユーザー指示、2026-08-11)
5. ~~3行クランプが実際には4行以上になっていた(バグ)~~ → `.tx`のflex化が`.clip3`のflexアイテム化を招いていた(flexとline-clampの既知の相性問題)ことが原因。`.tx`のflexを解除して修正(2026-08-11)
6. ~~「日付つきの注目ポイント」見出しが区分見出しの「リマインダー」と語が同期していない~~ → 「リマインダー登録」に変更、取り込み元の説明は案内文に移動(ユーザー指示、2026-08-11)
7. ~~「新しい指標名N」「読取N行」チップは判断材料にならない~~ → 削除(ユーザー指示、2026-08-11)
8. ~~凡例が表限定でない機能まで説明し、横一列で読みにくい~~ → 「セルをタップで全文表示」を削除、「削除対象」「新規追加」に簡潔化、項目ごとに改行(ユーザー指示、2026-08-11)
9. ~~「表記をそろえる指示をコピー」ボタンの枠線が主張しすぎ~~ → 既存の`.btn.ghost`(枠線なしパターン)に変更(ユーザー指示、2026-08-11)
10. ~~「調べることが思いつかない時は」が①②と同格の独立区分で、実際の依存関係(①の補助機能)と表示階層が一致していない~~ → ①の入れ子(「説明書の中身を見る」と同じ階層)に移動。展開記号も矢尻から銘柄詳細と同じ＋×に統一(ユーザー指示、2026-08-11)
11. ~~`#parseOut`の3区分(取り込み前確認・リマインダー/メモ確認・読み込み内容確認)の展開記号が矢尻のまま~~ → `parseGroup()`の記号を＋×に統一(ユーザー指示、2026-08-11)
12. ~~リマインダー/メモ候補の本文が太字で強調が強い、タップで続きが見られることが伝わりにくい~~ → 細字(400)に変更、既存の矢尻を流用したタップの目印を追加(実際に隠れている行がある場合のみ表示。ユーザー決定、2026-08-11)
13. ~~「似た名前の指標があります」がリマインダー/メモ選択セクションと構造が違う(独自の枠線・見出し・行・ボタン)~~ → 配色(amber)以外の構造(外枠・見出し・行・ボタン)を`.candbox`/`.rrow`/`.lnk.foot`の再利用で一致させた(ユーザー指示、2026-08-11)
14. ~~実行ボタンの文言が「読み込む」で画面名(取り込み)と語がずれていた~~ → 「取り込む」に変更(ユーザー指示、2026-08-11)
15. ~~読み込み内容確認の凡例が実態と不一致(青字=新規追加と表示していたが実際は修正時の追加文字。新規行そのものは緑背景のみで凡例に無かった)~~ → 青字ラベルを「修正」に、新規行の値を緑字にして凡例に「新規追加」として追加(既存の`--green-tx`を再利用。ユーザー指示、2026-08-11)
16. ~~「似た名前の指標があります」の見出し・説明文がamber文字色でリマインダー登録セクションより重く見えた~~ → 見出し・説明文の2箇所だけ文字色をリマインダー登録と同じ色(既存の`--ink3`/継承色)に変更。背景・枠線・個別行の警告・ボタンのamberは維持(ユーザー指示、2026-08-11)
17. ~~バグ: 「銘柄またはテーマ:」見出し(genOrderNewが対象未選択時に埋め込むプレースホルダー)がパーサーに認識されず「銘柄を特定できません」に落ち、新規銘柄・新規テーマの登録導線が出なかった~~ → 見出しの正規表現を「銘柄」「銘柄またはテーマ」「テーマ」のいずれでも解釈できるように拡張(コード付き=銘柄/コード無し=テーマ)。`lib/parseMd.js`と`index.html`内の独立実装の両方を修正(ユーザー報告、2026-08-13)
18. ~~未登録の銘柄・テーマが「新規銘柄として登録」ボタン→「取り込む」ボタンの2手順だった~~ → ボタンを削除し、「取り込む」を押した時点で未登録なら自動登録するよう統合(`registerStockFromImport`。ユーザー指示、2026-08-13)
19. ~~バグ: 同一文言のメモ候補が複数あると、片方をON/OFFすると見た目上の別行まで連動して動いた~~ → 署名(`memoSig`)に「配列中の出現順」を含め、行ごとに独立したON/OFF状態を持てるように修正(ユーザー報告・画像添付、2026-08-13)
20. ~~バグ: GPTが財務指標・業績推移の生数値を「## 分析」の中にサマリ/詳細の文章と一緒に出力すると、既存の分析サブ項目名と一致せずほとんどが「その他」扱いになった~~ → 「見出しが無い行の行き先をキーから推定」ヒューリスティックを「## 分析」配下にも拡張(既に分析サブ項目に一致する行は上書きしない)。`lib/parseMd.js`と`index.html`の両方を修正(ユーザー報告・実データ添付、2026-08-13)

### 2026-08-10(2回目)の12次元(4パターン方式)Phase1-2

11次元→12次元(データ整合性を追加)化・「機械測定群/理解のしやすさ/整合性/運用コスト」の4パターンに分けて観察した詳細は `DECISIONS.md` 同日エントリを参照。

- **視覚的ノイズ=2 → 3**: 装飾要素数(枠線+地色)を実測(0→10→86、3状態)し、件数に比例していて過剰でないことを確認。前回「見送り」としていたが、実測の結果、この周で3点に格上げする。
- **一貫性**: メモ候補カードと個別銘柄`hypoMiniCard`のcomputed style(背景・枠線・padding)が完全一致することを実測で確認。問題なし。
- **実運用摩擦**: `commitImport()`が取り込み完了後に必ず`openStock()`で銘柄詳細へ遷移する挙動について、複数銘柄を連続取り込みする使い方なら画面切替の反復になり得ると考え、ユーザーに実際の使い方を確認した。回答は「1回の会話で基本1銘柄」であり、現在の遷移は結果確認として適切と判断。**変更不要。**

### 2026-08-11 ユーザーの手動チェックで見つかった7件(Claudeの4パターン方式では見落とし)

詳細・見落とし理由の分析は `DECISIONS.md` 同日エントリを参照。手順書(`UI_REVIEW_LOOP.md`)にも
「パターン②③は機能グループを網羅する」を追加済み。

1. ~~認知負荷: 「新しい指標が○件増えます」が読み込み内容確認の表と重複表示~~ → 削除(件数はチップ、名前は表で見える)
2. ~~一貫性: コピーボタン(表記をそろえる指示・整形しなおすプロンプト)にアイコンが無くHome等と不統一~~ → `copyIconSvg()`を追加
3. ~~情報構造/一貫性: メモ候補がリマインダー候補と違い枠なしのプレーンラベルで「まとめただけ」に見えた~~ → `.candbox`を共有しカード化
4. ~~一貫性: リマインダー候補の日付/本文が独自クラス(`.d`/`.b`)で個別銘柄側と文字の見せ方が違った~~ → `.ttl`/`.meta`に統一
5. ~~一貫性: ON/OFFトグルが左側で一般的な配置(右側)と逆~~ → 右側に変更
6. ~~データ整合性: 「新しい指標N」チップが似た指標の数とも表の新規数とも合わない~~ → 「新しい指標名N」に改称し集計対象を明示
7. **判断保留(中立)**: 似た指標警告(`.metwarn`)の黄色背景の必要性 → 判断が要る項目への注意喚起として不合理ではないと判断し、今回は変更しなかった。再度指摘があれば取り上げる。

### 残っているもの(この周では見送り、ホームと同種の既存課題)
- **Color=2** … `--red-bg`(`chip.red`)が「銘柄を特定できません」警告・`需給確認`カテゴリタグ・選択数超過警告・`該当セクション不明`(orphan行、今回追加)の**少なくとも4つの異なる意味**で使われていることをgrepで確認した(`--amber-bg`の「似た指標警告」/「読めなかった行」の2重複と合わせ、既存の色トークン意味重複はこの画面に複数残っている)。Homeで見つかった「同色の二重の意味」と同型の既存課題で、実害(誤操作)は未確認のため、この周も見送り。次にColorを扱う周でHomeとまとめて棚卸しする。
- **Accessibility=2** … `.meta`/`.muted` 等(`--ink3`)のコントラスト不足。ホームで確認済みの全画面共通の既知の残課題(トークン定義の判断待ち、上記「ホームの未解決事項」参照)。

**着手時期についてユーザー決定(2026-08-11):** Color/Accessibilityのトークン棚卸しは、全画面のデザインブラッシュアップが一巡してから行う。理由: 未レビューの画面(市場・銘柄分析、リマインダー一覧・詳細、設定、編集シート群)で新しい色トークンの用法が今後増える可能性があり、先にトークン定義を直すと手戻り(設計見直し)になるため。それまでは両次元とも2点のまま保留する。

### 2026-08-13 見送った項目(「銘柄またはテーマ:」バグ修正時に提示したが選ばれなかった案)
- **`genOrderNew`のプレースホルダー文言修正**: AIへの指示文に埋め込まれる `# 銘柄またはテーマ: (会話の中で対象を明記してください)` を、「銘柄なら`# 銘柄:`、テーマなら`# テーマ:`を使う」という明確な指示文に直す案。今回はparseMd側の見出し認識を広げる対応を選んだため実施していない。同種の表記ゆれ報告が増えるようなら再検討する。
- **「特定できない」ときも新規登録を出すフォールバック**: 見出し行が全く無い(自由文だけの)貼り付けでも、貼り付け文の一部を仮の名称として「新規銘柄として登録」できる導線を追加する案。情報アーキテクチャ・主要ワークフローの追加にあたるため見送り。既存のプルダウンでの銘柄選択で対応可能。

### 2026-08-13 見送った項目(「## 分析」内の財務データ誤分類バグ修正時)
- **登録済み指標名が複数年の値を持つ場合の表示**: 「## 分析」救済ヒューリスティックで指標(`指標`ブロック)として救い出された行が、GPTの出力形式によっては複数年分の値("25年3月期=87.3 | 26年3月期=88.7")をそのまま1つの指標欄に格納してしまうケースがある(自己資本比率など、単年スナップショットを想定した指標名に複数年データが来た場合)。「その他」に落ちて完全に失われるよりは良いが、指標タイルの表示としては不自然。今回のバグ報告(その他扱いの解消)の範囲外のため見送り。次に指標表示を扱う周の課題として残す。
- **`genOrderNew`のプロンプトテンプレート強化**: 「財務指標・業績推移は必ず## 業績推移 / ## 指標の見出しの下に書く。## 分析にはサマリ・詳細の文章のみ書く」という指示を追加する案。パーサー強化で対応したため実施していないが、GPTの出力揺れは今後も起き得るため、同種の報告が増えるようなら合わせて検討する。

## 取り込み画面: 新方式での初回レビュー(2026-08-15)

`UI_REVIEW_LOOP.md`の改訂(実運用規模fixture・Phase 1.5a/1.5b)後、最初の実施対象としてユーザーから
指定された。`test/e2e/fixtures/realdata.json`(日東工器6151相当、業績推移20指標)を用い、
新規銘柄への初回取り込み・既存銘柄への更新取り込みの両方を実際にPlaywrightで通した。
まだPhase 3(仮説選択・実装)は行っていない。**課題抽出(Phase 0〜2)のみ。**

### Phase 1 スコアカード(4パターン方式、抜粋)
観察した状態: 通常(testdata.json) / ゼロ状態(カード0件) / 多件数(realdata.json、新規・更新の両方) / 2回目(画面の閉じ直し)

| 次元 | 点 | 根拠(実測値) |
|---|---|---|
| Typography/Spacing/Hit Area/Color | 3 | `design.js`/`colortoken.js`/`sheettap.js` green(既存の機械測定群、変更なし) |
| Accessibility | 3 | 警告バナー(読めなかった行・似た指標)は既存の`.banner.warn`パターンを踏襲、コントラスト等は`contrast.js`でカバー済み |
| 情報密度 | 2 | 業績推移の更新前/更新後が5年分の値をまるごと1セルに連結して表示し(例: 「23年3月期=27,256｜24年3月期=27,289｜…｜27年3月期=33,800」)、実際に変わった年度がどこかを文字列全体から目視で探す必要がある(実データで実測) |
| 視覚的ノイズ | 3 | 既存パターン(banner/chip)の再利用のみで新規の視覚要素は無い |
| 認知負荷 | 2 | ②に貼り付け(または入力)後、`parseAndPreview()`はプレビューを`#parseOut`に描画するが自動スクロールが無く(コード上`scrollIntoView`等の呼び出し無し)、①の説明書アコーディオンが開いていると特に、結果に気づくまで手動でスクロールする必要がある(実測: 貼り付け直後は画面上部のまま) |
| 情報構造 | 3 | ①②の区分、プレビュー内の3区分(読めなかった行/メモ候補/内容確認)は明確で階層も一貫 |
| 一貫性 | 2 | 説明書内の「複数件のときの書き方」指示が、注目ポイント(「## 注目ポイントの見出しごと繰り返す」)とメモ(「- 本文の行を繰り返す」)で異なる形式を要求している。実際に「本文:」プレフィックスの無い注目ポイントの箇条書きを貼ると、2件とも「読めなかった行」に落ちることを実測で確認した(回復手段の`整形しなおすプロンプト`は原文を自動で埋め込んでおり機能することも確認済み) |
| データ整合性 | 3 | 新規登録・既存更新のいずれも、コミット後の実データ(`stock().metrics`/`.trend`/`.sections`)が確認テーブルの表示と一致することを実測で確認。読めなかった行の検出も0件の誤判定なし |
| 実運用摩擦 | 2 | 上記「認知負荷」の自動スクロール欠如、および「一貫性」の書式非対称による往復1回増(整形しなおし)の2点が該当 |

**最低の重要次元: 認知負荷=2 / 一貫性=2 / 情報密度=2 / 実運用摩擦=2(0点・1点なし)。**

### Phase 1.5a 通し実行で引っかかった瞬間
- [②貼り付け直後] 貼り付ければ結果が見えると期待したが、画面はそのままで、下にスクロールしないと「読めなかった行」「メモ候補」「読み込み内容の確認」「取り込むボタン」のいずれにも気づけなかった
- [読めなかった行] 注目ポイントを「本文:」プレフィックス無しの素の箇条書きで書いたところ、2件とも「読めなかった行」に分類された。指示文どおり(「- 本文: 」形式)なら問題なく読める(別途確認済み)
- [読み込み内容の確認] 5年分の業績推移を持つ既存銘柄の更新で、実際に変わったのは最新1年分の予想値だけだったが、更新前/更新後の表示は5年分を1本の文字列として並べており、どこが変わったのか一目では分からなかった
- [ヘッダー無しの続きの貼り付け] 「# 銘柄:」見出しの無い続きの回答を、銘柄詳細を経由せず直接この画面から貼った場合の挙動を確認した → 「銘柄を特定できません」+選択式プルダウン+次回からの回避方法の案内が出ており、**想定より良く出来ていた(欠陥ではない)**
- [一括操作] メモ候補・リマインダー候補にはそれぞれ既存の一括ON/OFFボタンがあり、「次回から確認せずすべてオンにする」の記憶設定もあった → **想定より良く出来ていた(欠陥ではない)**

### Phase 1.5b オポチュニティレンズ
1. **記憶・再入力**: 「整形しなおすプロンプト」の実装(`copyReformat()`)を読んだ。失敗した原文(`window._failed`)を自動で埋め込んでおり、ユーザーが原文を再入力する必要は無かった。**候補: 無し(確認済み)。**
2. **反復**: `bulkMemoPick`/`bulkRemPick`(一括ON/OFF)と`setAutoRemind`(次回から確認省略)の実装を読んだ。候補にしようとした「メモ・リマインダー候補を1件ずつ確認する反復」は、既にこの2つで対応済みだった。**候補: 無し(確認済み、既に対応済み)。**
3. **手作業の残存**: 実データ(realdata.json、6151・業績推移20指標5年分)で実際に更新プレビューを生成し、目視で確認した。
   - 候補: 業績推移の更新前/更新後で、**変更のあった年度だけ強調表示**する(未変更の年度は現状どおり簡潔に、または折りたたむ)
   - 効果: 四半期ごとの定点更新(このアプリの主要な使い方)のたびに、5年分の文字列から1箇所の変更を目で探す作業が発生することを実データで確認した。決算のたびに繰り返す操作なので、恩恵は反復的
   - コスト: 差分は年度キーごとのオブジェクト比較で軽い。表示側(確認テーブルのセル生成)の改修のみで、データ構造・保存形式は変わらない
   - 代替: 既存の確認テーブルの表現を変えるだけで足り、新しい画面や入口は不要
   - 判定: **採用候補**(実データでの反復操作という一次証拠があり、コストも小さい)。表示のみの変更だが「業績推移テーブルの区切り線」修正時と同様、実装時に既存トークン・既存パターンからの逸脱が無いか確認する

### 解消済み(2026-08-15、ユーザー指示「全て実装しましょう」)
1. ~~認知負荷/実運用摩擦=2: 貼り付け後のプレビューに自動スクロールしない~~ → 空→非空の遷移時だけ`#parseOut`へ自動スクロールするよう修正(`DECISIONS.md`同日エントリ参照)
2. ~~一貫性=2の一部: 注目ポイントがメモと違い「本文:」ラベル無しの書き方を受け付けず、読めなかった行に落ちていた~~ → メモと同じ救済(ラベル無しでも`本文`として受ける)を追加。**ただし説明書の指示文自体の非対称は残っている(下記に記録)**

### 解消済み(2026-08-17、ユーザー承認A案「年度単位マージ」)
- ~~業績推移の部分更新で、貼り付けに無い年度のデータが削除される~~ →
  `commitImport()`の業績推移取り込みを「指標ごと丸ごと置き換え」から「年度ごとの置き換え」に変更。
  共有ヘルパー`mergeTrendYear()`/`trendObjToStr()`をプレビューと実際の保存の両方から呼び、
  表示と保存結果が食い違わないようにした(実データ検証・A/B/C案の提示・ユーザー承認の経緯は
  `DECISIONS.md`の2026-08-15(7)エントリ、実装は2026-08-17エントリを参照)。

### 解消済み(2026-08-17、指示文の非対称を解消)
- ~~一貫性=2: 説明書の「複数件時の書式」指示が、注目ポイント(見出しごと繰り返す)とメモ(本文の行を繰り返す)で非対称なまま~~ →
  注目ポイントの指示文をメモと同じ「「- 本文:」の行を繰り返す」表現に統一(パーサーの実装=本文単位のグループ化に合わせた)。
  詳細は`DECISIONS.md`の2026-08-17(2)エントリを参照。

### 解消済み(2026-08-17、Phase 1.5b採用候補「変更年度ハイライト」の実装)
- ~~情報密度=2: 業績推移の更新前/更新後が複数年分の値を1本の文字列に連結して表示し、実際に変わった年度がどこかを目視で探す必要がある~~ →
  `trendYearDiffHtml()`を新設し、年度ごとに前後を比較して変わった年度だけを既存の差分色(更新前=`d-del`、更新後=`d-add`)で
  強調するよう変更。未変更の年度は引き続きプレーン表示。詳細は`DECISIONS.md`の2026-08-17(3)エントリを参照。

### 残っているもの
1. Color=2・Accessibility=2は既存の全画面共通の保留課題のまま(2026-08-11ユーザー決定どおり据え置き)。取り込み画面固有の課題はこの周ですべて解消し、12次元が3点になった(`DECISIONS.md`同エントリのSelf-Critique参照。他画面共通の据え置き2件は対象外)

## 取り込み画面: 2周目レビュー(2026-08-20、配色系の視認性と画面内一貫性に着目)

前項までで取り込み画面固有の12次元は3点まで解消したが、「同じ画面をもう1周確認して新規の課題が見つかるか」というユーザーの依頼(配色系の視認性・他画面との違和感を特に意識)により再度Phase 0〜2(課題抽出のみ、実装はまだ)を実施した。realdata.json(6151)・testdata.json(9001)の両方で、実測(コントラスト計算・DOM構造比較・タップ挙動の実行)に基づき観察した。**この周では実装していない。以下は発見の記録であり、実装するかはユーザー判断待ち。**

### 見つかったもの(すべて新規発見。前2周のレビュー・実装では触れていない箇所)

1. **一貫性=2(新規発見・既存の構造): 確認テーブルのセル(更新前/更新後)は実際にタップで展開できるのに、視覚的なタップ目印(矢印)が無い。同じ`#parseOut`内のメモ/リマインダー候補行(`.candbox`)には目印が付く。**
   - 実測: 3行を超える長文を確認テーブルのセル・候補カード双方に用意し、同一画面・同一出力内で比較した。候補カード側(`.candbox .ttl.clip3`)は`markClip3Truncated()`が`.tapmark`(矢印)を付ける。確認テーブル側(`<td><div class="clip3">`)は実際に3行を超えて切り詰められている(`scrollHeight`実測で確認)にもかかわらず、`markClip3Truncated()`のクエリが`.candbox .ttl.clip3`に限定されているため対象外で、矢印が一切付かない。`showCell()`自体は両方で同じ関数が使われ、タップすれば実際に展開はできる(実測で確認)。タップ領域のサイズは問題ない(実測 65×222px、44px下限を大きく超える)。
   - 影響: 機能は壊れていないが、発見しにくい。特に業績推移の確認テーブルは今回(2026-08-17)の年度ハイライト実装で複数年度をまたぐ長い文字列になりやすく、切り詰めが起きる頻度も上がっている。
   - 新規/既存: 構造自体(`markClip3Truncated`のクエリ限定)は今回のPRより前から存在する既存の欠陥。今回新たに顕在化させたわけではないが、直近の年度ハイライト実装で切り詰めの発生頻度が上がった可能性がある。

2. **Color/Accessibility=2(既存の全画面共通課題の具体的な再確認・数値の更新): `.d-del`(取り消し線+`--ink3`)は実測コントラスト比2.86:1で、業績推移の「更新前」セルではこの低コントラストな取り消し線テキストが変更前の実際の数値という主情報を担っている。`.metwarn .d`(似た指標の説明文、`--ink3` on `--amber-bg`)は実測2.69:1でさらに低い。**
   - 実測: Playwrightで実際にレンダリングされた`color`/`background-color`(祖先を辿って実背景を取得)からWCAG相対輝度式でコントラスト比を計算。`.d-del`は`rgb(139,149,161)` on `rgb(247,248,249)` → 2.86:1(AA基準4.5:1未達、AA-large基準3:1にも未達)。同じ画面内の`.d-add`(6.68:1)`.d-new`(6.23:1)`.chip.green`(6.23:1)は十分な数値で、`.d-del`だけが際立って低い。
   - 既存/新規: `--ink3`のコントラスト不足自体は2026-08-11に発見済みで、Color=2・Accessibility=2として全画面共通の既知の課題(ユーザー決定により据え置き中)。今回**新しいのは「主情報(変更前の実数値)に使われている具体的な箇所を特定したこと」**。従来の`--ink3`低コントラストの指摘は`.hint`や`.meta`等の補助テキストが中心で、「読めなくても致命的ではない」ケースが多かったが、`.d-del`は「この年度は元々いくつだったか」という、更新内容を判断する上で読む必要がある数値そのものに使われている点が異なる。据え置き決定を覆すかはユーザー判断だが、判断材料として記録する。

3. **プロセスの見落とし(記録漏れ): 2026-08-17(3)のSelf-Critiqueで指摘した「凡例(`difflegend`)が業績推移の『年度が置き換わる』という意味まで説明していない」が、`REVIEW_BACKLOG.md`の「残っているもの」に記録されていなかった。**
   - `DECISIONS.md`2026-08-17(3)のSelf-Critiqueには記述があるが、`UI_REVIEW_LOOP.md`「Phase 2」の「この周で直さないものは`REVIEW_BACKLOG.md`に残す」を実施し忘れていた。凡例は現在も「新規追加/修正/削除対象/既存指標に該当せず」という汎用文言のみで、業績推移特有の「年度が置き換わる(指標ごと消えるわけではない)」という意味までは説明していない(実測: `difflegend`のHTML出力を確認)。

### Phase 1.5a ワークフロー通し実行で引っかかった瞬間
- [確認テーブル] 長い分析詳細文を貼ったところ、セルが3行で切り詰められていたが、タップできる目印が無く、候補カード側の矢印と見比べて初めて「ここも本当はタップできるのでは」と気づいた(=上記1)
- [業績推移の複数年度同時変更] 2つの年度を同時に変更するテキストを貼ったところ、両方の年度が正しく個別に強調され、想定通り動作した(**想定より良く出来ていた(欠陥ではない)**。単年度しかテストしていなかったため今回追加で確認)
- それ以外は前2周までの対応で解消済みの箇所を再確認したのみで、新たに引っかかった瞬間は無し

### Phase 1.5b オポチュニティレンズ
1. **記憶・再入力**: `copyReformat()`(失敗行の自動埋め込み)を再確認。前回確認済みの内容から変化なし。**候補: 無し(確認済み)。**
2. **反復**: `STATE._importPick`/`STATE.askStockId`の実装を読んだ。タブバーから直接「会話を取り込む」を開くと(`STATE._askFromStock`がfalseの場合)、銘柄選択は毎回リセットされ、プルダウンから選び直す必要がある(`go()`のimportタブ処理、line 887/890)。
   - 候補: 直前に取り込んだ銘柄をプルダウンの初期選択にする
   - 効果: 同じ銘柄へ続けて複数回取り込む場合(決算シーズンに指標を分けて貼る等)は選び直しの手間が減る。ただし、このアプリの主要な使い方は「1回の会話でその銘柄の分析をまとめて貼る」設計(`briefingText()`は複数指標・複数分析項目を1回でまとめて出力するよう促す文面)であり、同一銘柄への連続再訪がどれだけの頻度で起きるかは実データ・ログが無く確認できない
   - コスト: 実装は小さい(`STATE._importPick`の初期値を最後の取り込み銘柄にするだけ)
   - 代替: 銘柄詳細から「会話を取り込む」を開けば(`openAsk()`)その銘柄が自動選択される既存導線があり、同一銘柄への連続取り込みはこちらを使えば足りる
   - 判定: **見送り**。既存の代替導線(銘柄詳細経由)で足りており、実利用頻度の裏付けが無いまま実装すると、逆に「前回と違う銘柄のつもりが前回の銘柄が選ばれたままだった」という誤操作リスクを新たに作りかねない
3. **手作業の残存**: 「似た名前の指標があります」warning box(`.candbox.metwarn`)の実装を読んだ。ボタンは「表記をそろえる指示をコピー」(`copyMetFix()`、外部AIへの指示文をコピーするだけ)のみで、その場で直接まとめる操作は無い。まとめる操作自体は別画面(指標設定シートの`sheetMetricEdit()`→「ほかの指標にまとめる」)にあり、この警告ボックスからの直接リンクは無い。
   - 候補: 警告ボックスの各行から、対応する指標の`sheetMetricEdit()`へ直接遷移するリンクを追加する
   - 効果: 「表記揺れは直したいが、外部AIに聞き直すほどではなく、単に同じ指標としてまとめたいだけ」の場合に、指標設定シートを自力で探す手間が減る。ただし、この警告が実際にどのくらいの頻度で「AIへの指示コピー」と「直接まとめ」のどちらを選びたい状況で出ているかは実データが無い
   - コスト: 指標名・種別(`kind`)は警告データに既に含まれており、`sheetMetricEdit(kind, name, src)`へのリンクを1行追加するだけ。データ構造・保存形式への影響は無い
   - 代替: 銘柄詳細→指標設定シートという既存導線はある(遠回りだが到達可能)
   - 判定: **採用候補**(実装コストが小さく、既存導線はあるが遠回りという明確な欠落がある)。ただし優先順位は上記1(一貫性)・2(Color/Accessibility)の欠陥より低い(`DESIGN_PRINCIPLES.md`7の優先順位により、既存の欠陥を機会候補より優先する)

### スコアカード再測定(前項2026-08-17(3)からの差分のみ再測定、他は転記)

| 次元 | 点 | 根拠(実測値) | 新規/既存 |
|---|---|---|---|
| 認知負荷 | 3 | 前回と変化なし(前項からの転記)。上記1は一貫性側で計上 | 既存 |
| 情報構造 | 3 | 前回と変化なし(前項からの転記) | 既存 |
| 視覚的ノイズ | 3 | 前回と変化なし(前項からの転記)。複数年度同時変更でも強調表示は過剰にならないことを実測で確認(上記Phase 1.5a) | 既存 |
| 情報密度 | 3 | 前回と変化なし(前項からの転記) | 既存 |
| Typography | 3 | 新しい強調表示(`.d-add`/`.d-del`)のfont-sizeは11px/12pxで既定6段階内(実測) | 既存 |
| Spacing | 3 | 前回と変化なし(前項からの転記) | 既存 |
| Hit Area | 3 | 確認テーブルのセルのタップ領域を実測(65×222px)、44px下限を大きく超える。上記1は目印の有無の問題であり、領域サイズの問題ではない | 既存 |
| Color | 2 | `.d-del`実測2.86:1、`.metwarn .d`実測2.69:1。いずれもWCAG AA(4.5:1)未達。既存の全画面共通課題(`--ink3`)の具体的な再確認(上記2)。据え置き決定は維持するが、主情報に使われている点を新たに特定 | 既存 |
| Accessibility | 2 | 上記Colorと同根拠。`colortoken.js`/`contrast.js`はこのパターン(`.d-del`・`.metwarn .d`)を検査対象にしていない(機械検査の対象外であることも確認) | 既存 |
| 一貫性 | 2 | 確認テーブルのセルと候補カードで、同じ「タップで展開」という操作の視覚的表現が異なる(上記1、実測で確認)。前項では3点としていたが、今回の実測で見落としが判明したため見直す | 既存(前項の見落としを修正) |
| 実運用摩擦 | 3 | 前回と変化なし(前項からの転記) | 既存 |
| データ整合性 | 3 | 前回と変化なし(前項からの転記) | 既存 |

**最低の重要次元: Color=2 / Accessibility=2 / 一貫性=2(0点・1点なし)。**

### 解消済み(2026-08-20、ユーザー承認により実装)
1. ~~一貫性=2: 確認テーブルのセルにタップ目印(`.tapmark`)が無い(上記1)~~ →
   `markClip3Truncated()`のクエリを`#parseOut table .clip3`にも拡張し、候補カードと同じ目印を追加。
   対策前後のスクリーンショットをユーザーに提示し承認を得て実装。詳細は`DECISIONS.md`の2026-08-20エントリを参照。
2. ~~Color/Accessibility=2の一部: `.d-del`(業績推移「更新前」セルの取り消し線)が`--ink3`で実測2.86:1、WCAG AA未達(上記2)~~ →
   `--ink2`に変更し実測5.52:1に改善。`--ink3`全画面共通の据え置き決定自体は覆さず、主情報を担う`.d-del`だけを局所修正。
   `.metwarn .d`(実測2.69:1)は今回のサンプル提示の対象外だったため未対応のまま残す(下記)。
   対策前後のスクリーンショットをユーザーに提示し承認を得て実装。詳細は`DECISIONS.md`の2026-08-20(2)エントリを参照。
3. ~~凡例(`difflegend`)が業績推移特有の意味を説明していない(上記3、記録漏れの是正)~~ →
   業績推移の行があるときだけ「業績推移は年度ごとに置き換わります」という補足を凡例に追加。
   詳細は`DECISIONS.md`の2026-08-20(3)エントリを参照。

### 解消済み(2026-08-22、ユーザー指摘・承認済みモックアップにより実装)
4. ~~「読めなかった行」のエリアが「似た名前の指標があります」とデザインが違う(独自の`.failbox`構造)、および取り込み画面全般の枠線・区切り線が多くビジー感がある~~ →
   `.candbox`から外枠(border)と行区切り線(`.rrow`/`.lnk.foot`のborder-top)を廃止し、見出し(`.cbx-head`)/内訳(`.cbx-body`)の二層背景に統一。「読めなかった行」も`.failbox`から`.candbox.metwarn`(似た指標と同じ構造)に置き換え、似た指標/読めなかった行/メモ候補の3種で完全に同じHTML構造・CSSクラスになった。
   併せて`.metwarn`の見出し背景をamber系から他の2種と同じ中立色(`--bg`)に統一(ユーザー指示)。詳細は`DECISIONS.md`の2026-08-22エントリを参照。

### プロセスの欠陥(2026-08-22、ユーザー指摘から判明。次周の対策対象)

**「枠線・区切り線が多くてビジー感がある」を、自力で課題として抽出できなかった。**
ユーザーに指摘されて初めて着手し、2026-08-22のPR #155で解消した。問題そのものは解消したが、
**検出できなかったこと自体**が対策対象として残る。

- **原因(特定済み):** `DESIGN_SCORECARD.md` の「視覚的ノイズ」の測定方法が
  「スクリーンショットを比較し、装飾要素の**追加・削除**を確認」となっており、
  **その周の差分しか見ていない**。各周で新しい枠線を足していないので毎回「視覚的ノイズ=3」が付き続け、
  一方で線の**絶対量**は周をまたいで蓄積していた。差分を見る指標では、蓄積して閾値を越える種類の
  劣化は原理的に検出できない。
- **関連する構造的欠落(デザインハーネス4層モデルとの照合で判明、2026-08-22):**
  - 検証の段階2「スクリーンショットで検証(密度・階層・既存画面との差分)」が存在しない。
    `test/e2e/` に6枚のPNGをコミットしているが、ベースライン比較をするスイートは0件で、
    実行のたびに `git checkout -- *.png` で差分を破棄している(実測で確認)。
  - 検証の段階1「ルールで検証」に**コンポーネント**の検査が無い。色・文字サイズ・タップ領域は
    全て合格していたため、同じ役割のカードが2実装(`.failbox` / `.candbox`)並存しても検出されなかった。
- **対策の方向(未着手):**
  1. 視覚的ノイズの測定方法に、差分だけでなく**絶対量の測定**を加える →
     **2026-08-22に対応済み。**`test/e2e/visualnoise.js` が主要6状態の線(border)の本数を
     実レンダリングから数え、記録値との**完全一致**を検査する(減った場合も記録の更新を強制する。
     古い値を残すと、その差分ぶんを無検査で積み増せてしまうため)。
     `DESIGN_SCORECARD.md` の「視覚的ノイズ」の測定方法も、絶対量と差分の2本立てに改めた。
     この指標が元の問題を捉えられることは実測で確認済み(取り込み画面・貼り付け後が
     PR #155 の前 62本 → 後 41本。同時に要素総数は 180 → 198 と増えており、
     要素数ではなく線の量を測れている)。
  2. スクリーンショットのベースライン比較を導入する →
     **2026-08-22に対応済み。**`test/e2e/shotmetrics.js` が `#view` の画素から
     `ink`(地色以外の割合)・`colored`(有彩色の割合)を、DOMから `scrollHeight` を測り、
     記録値と照合する。画像そのものを比較するのではなく、画像から取った量を比較する方式にした
     (画素完全一致はアンチエイリアスや環境差で落ちやすく、落ちたときに何が変わったのかも読めないため)。
     `visualnoise.js`(線の本数)と測るものが違うことは実測で確認済み: **色だけを変えて線を
     1本も動かさない変更**(警告カードの地色をamberに戻す)は `visualnoise.js` を素通りし
     (41本のまま合格)、`shotmetrics.js` だけが捉えた(`colored` 10.05% → 26.55%)。
  3. コンポーネントの一次定義を `DESIGN_SYSTEM.md` に持つ →
     **2026-08-22に対応済み。**`DESIGN_SYSTEM.md`「Components」を新設(18項目)し、
     `test/e2e/component.js` でカード状コンテナの登録検査を追加した。

### コンポーネント文書化の範囲外にしたもの(2026-08-22、次周以降の候補)

`component.js` の検出条件(単独クラスのセレクタで `border-radius` と `background:var(--*)` を
両方持つ)には当たるが、`DESIGN_SYSTEM.md` への一次定義は持たせなかったもの。
**小さな操作部品か、用途が1つに固定されていて「使い分けを誤る」余地が無いもの**という基準で外した。
`component.js` の `UNDOCUMENTED_OK` に列挙してあり、増減の検査自体は効いている。

- `.btn` / `.sw` / `.dot` … 小さな操作部品(ボタン・トグル・ページャの点)
- `.exit-confirm-card` / `.mine` … 用途が1つに固定された表示

**2026-08-22追記(判断確定):** このうち `.toast` と `.nodata` は文書化した。どちらも
「似ているが使い分けが要る」相手がいるため(`.toast`↔`.banner` は消える/消えない、
`.nodata`↔`.empty` は枠の中が空/画面全体が空)、選択を誤る余地が実際にある。
残る5つは選択肢が無く、一次定義を置いても守るべきことが増えないため、**文書化しないで確定**とする
(一次定義は「複数の選択肢から誤ったものを選ばないため」に置くものという整理)。
増減の検査は `component.js` の登録一覧で引き続き効いている。

### 今回直さなかったもの(次周へ送る)
1. Color=2・Accessibility=2: `.metwarn .d`(似た指標・読めなかった行の説明文)の低コントラスト(実測2.69:1、`.d-del`と同根拠の`--ink3`問題)。2026-08-22で`.metwarn`の背景色をamberから`--bg`に変えたが、`--ink3`自体のコントラスト不足は背景色によらず解消しないため引き続き未対応(`DECISIONS.md`2026-08-22エントリで再計算・再確認済み)
2. 機会候補(採用): 「似た名前の指標があります」警告から指標設定シートへの直接リンク追加(上記Phase 1.5b-3)

## 取り込み画面: 3周目レビュー(2026-08-22、①からの通し実行)

ユーザー依頼「①からすべて通しでテストして課題抽出」。Phase 0〜2 + 1.5a/1.5b を実施。
**この周では実装していない。以下は観察の記録であり、実装するかはユーザー判断待ち。**

観察に使ったのは `realdata.json`(6151 日東工器への更新取り込み、確認テーブル20行・
メモ候補4件・リマインダー候補3件)と、ゼロ状態として 4062「まだ空の銘柄」への初回取り込み。
貼り付け内容は説明書のフォーマットに沿った実運用規模のもので、表記ゆれ1件・
説明書が禁じている出典/確信度・見出し外の自由文を意図的に混ぜてある。

### Phase 1 スコアカード

観察した状態: 通常(realdataの6151へ更新取り込み) / ゼロ状態(4062へ初回取り込み、貼り付けを空にした直後) /
多件数(確認テーブル20行・メモ候補4件・リマインダー候補3件) / 2回目(取り込み→再訪→同じ内容を再貼り付け)

| 次元 | 点 | 根拠(実測値) | 新規/既存 |
|---|---|---|---|
| 認知負荷 | 2 | ゼロ状態で「取り込むリマインダー/メモを**選択してください**」区分が出るが、中身は「メモ候補 0件 / 今回はありません。」だけで選択対象が0件。指示形の見出しがカード1枚分を占有する(実測) | 新規発見 |
| 情報構造 | **1** | 確認テーブルが幅560pxで、表示枠`.xscroll`の358pxに収まらない。**全20行の「更新後」列が186pxはみ出す**(実測)。画面内に見えるのは更新後セルの先頭36pxのみ。区分名は「読み込み内容を確認してください」だが、確認すべき更新後の値が初期表示で1行も読めない | 新規発見 |
| 視覚的ノイズ | 2 | 業績推移行の「更新前」セルが、5年分を1本に連結した文字列に取り消し線が多数かかり3行クランプされる。2026-08-17(3)の年度ハイライトは、変更年度が多いと逆にノイズになる(目視・スクリーンショット) | 新規発見 |
| 情報密度 | 2 | 貼り付け後の高さ3167px=**3.8画面**。「✓ 取り込む」のY座標3085(ページ全体3219)でほぼ最下部。同じ内容を再貼り付けしても20行中17行が「変更なし」のまま同じ高さの表を再表示(実測) | 既存 |
| Typography | 3 | この画面のfont-sizeは11/12/13pxの3種類のみ。契約(6種類以内・11px以上)を満たす(実測) | 既存 |
| Spacing | 3 | `design.js` 観点F green。新規gap値なし | 既存 |
| Hit Area | **1** | `.swtap`(トグルのタップ領域)が短い行で**36px/28px**、`.tx`(本文のタップ領域)が**18px**と44px下限を割る(実測)。`.swtap`は2026-08-11に「行の高さいっぱいに広げ、トグルの見た目(44x26px)より実際のタップ領域を大きく保つ」意図で`align-self:stretch`にしたが、1行だけの候補では行自体が44px未満になり意図が破れている。`design.js` 観点Dの検査対象(`#view button`/`.list-row`/`.sub-row`/`.sec .h`)に`.swtap`/`.tx`/`.sw`が入っておらず検出されていなかった | 新規発見 |
| Color | 2 | AA未達を9種類実測(すべて11px・`--ink3`系): `.metwarn .d` 2.66 / `.candbox .meta`(リマインダーの日付)3.04 / `th`(表の見出し「項目」)2.86 / `.note`(業績推移の注記)2.86 / `.sep`(凡例の「\|」)**1.46** / `.muted` 2.66〜2.86 / `.d-new`の補助行 2.6 | 既存(トークン判断待ち)+ 箇所を新規特定 |
| Accessibility | **1** | 上記Hit Areaの44px違反と、Colorの実測AA未達9種類。特に`th`「項目」と`.meta`の日付は補助ではなく判断に使う情報 | 新規(Hit Areaにより1へ) |
| 一貫性 | 2 | (a)「📋 説明書をコピー」と「📋 貼り付ける」が同じ絵文字・同じ`.btn.pri`(全幅紺ベタ)で、**出す/入れるという真逆の操作が同じ見た目**。(b)コピー操作のアイコンが絵文字(📋)とSVG(⧉)で混在。(c)件数の書式が「(1)」と「N件」の2種類。(d)銘柄詳細の業績推移テーブルには1列目固定を入れた(2026-08-20(4))が、取り込みの確認テーブルには同じ手当てが無い(実測) | 新規発見 |
| 実運用摩擦 | 2 | (a)貼り付けると貼り付け欄が画面外(top=-96px)へ出る。説明書は「分割して届いた場合は続けて貼り付ければ追加されます」と案内しているが、追加貼り付けには上へ戻る操作が要る(実測。スクロール主体は`#view`ではなく`document`)。(b)確認テーブルの更新後を読むには全20行で横スクロールが要る | 新規発見 |
| データ整合性 | **1** | 下記3件(実測) | 新規発見 |

**最低の重要次元: 情報構造=1 / Hit Area=1 / Accessibility=1 / データ整合性=1(0点なし)。**

### データ整合性=1 の内訳(3件)

1. **カードは「リマインダー登録 3件」と表示するが、実際に登録されるのは2件。**
   日付つき注目ポイント3件のうち2件が同じ日(2026/11/6)でまとまるため。実測で `DB.reminders` は 4→6(+2)。
   説明文に「同じ日は1つにまとまります」とは書いてあるが、見出しの「3件」が候補行数であって
   登録件数ではないことは読み取れない。
2. **説明書が禁じている「出典・確信度」が、メモ候補として既定ONで取り込まれる。**
   説明書は「出典・URL・確信度は書かないでください」と明示し、`commitImport()`のコメントも
   「出典も確信度も保存しない。どちらも生成AIの自己申告で…信頼の手掛かりにならない」と書いている。
   にもかかわらず、AIが書いてしまった場合はメモ候補になり、既定ONのまま取り込むと
   `DB.hypotheses`(kind:'memo')に保存される(実測)。**設計意図と実装が矛盾している。**
   なお2行(出典・確信度)が1つの候補に結合される。
3. **表記ゆれを見逃して取り込むと、2回目以降は警告が出ない。**
   「配当利回り」(既存は「配当利回り(予想)」)を取り込むと登録済みになり新規でなくなるため、
   同じ内容を再貼り付けしても「取り込み前に確認してください」区分ごと消える(実測)。誤りが固定化する。

### Phase 1.5a ワークフロー通し実行で引っかかった瞬間(8件)

1. [初回表示] 「📋 説明書をコピー」と「📋 貼り付ける」が同じ絵文字・同じ紺ベタで縦に並び、どちらを先に押すのか迷った
2. [貼り付け直後] 貼り付け欄が画面外へ出て、追加で貼りたいときの戻り方が分からなかった
3. [確認テーブル] 「読み込み内容を確認してください」と言われたのに、更新後の値が読めなかった
4. [業績推移の行] 取り消し線だらけの連結文字列で、何がどう変わるのか読み取れなかった
5. [リマインダー] 3件オンにしたのに、取り込み後に増えたのは2件だった
6. [メモ候補] 説明書が「書かないでください」と指示している出典・確信度が既定オンで入っていた
7. [ゼロ状態] 「選択してください」と言われたが、選ぶものが1つも無かった
8. [2回目] 同じ内容を貼り直したら、1回目に出ていた表記ゆれの警告が消えていた

### Phase 1.5b オポチュニティレンズ

1. **記憶・再入力**: 表記ゆれを直すには「表記をそろえる指示をコピー」→外部AIに貼る→直った回答を貼り直す、という往復が要る。
   - 候補: 似た指標の行から直接「この指標にまとめる」操作(`sheetMetricEdit`へのリンク)
   - 効果: 往復1回ぶんの削減。**2周目の審査時より価値が上がっている**(上記データ整合性3により、
     1回見逃すと2回目以降は警告自体が出ないため、その場で直せることの重みが増した)
   - コスト: 小〜中(指標名・種別は警告データに含まれ、統合ロジックは`sheetMetricEdit`に既存)
   - 代替: 銘柄詳細→指標設定シート(遠回りだが到達可能)
   - 判定: **採用候補**(2周目からの持ち越し、優先度は上昇)
2. **反復**: 銘柄選択のリセット(2周目で「見送り」と判定済み)。前提に変化なし。**候補なし。**
3. **手作業の残存**: 確認テーブルの更新後を読むために全20行で横スクロールが要る。
   - 候補: (A)1列目固定 (B)更新前/更新後の縦積み (C)変更のある行だけ更新前を出す
   - 効果: 横スクロール操作の消滅または軽減
   - コスト: 中〜大。BとCは表の意味・情報構造に触れる
   - 代替: 現状は横スクロール可能なので機能自体は壊れていない
   - 判定: **要ユーザー判断**(情報構造の変更にあたるため自律で決めない)

### 解消済み(2026-08-22、ユーザーがA案を選択)
1. ~~情報構造=1: 確認テーブルの「更新後」列が全20行で画面外にはみ出し、初期表示で読めない~~ →
   `table.diffpv td:first-child`に`position:sticky; left:0`を追加し、1列目(項目名)を固定した。
   横スクロール中も項目名が枠の左端に留まり、どの行の値かを見失わなくなった(実測: scrollLeft 0→202で
   1列目の左端は16pxのまま、更新後セルの右端は576px→374pxで画面内)。詳細は`DECISIONS.md`の2026-08-22(2)。
   **ただし横スクロール自体は残り、更新後の値は初期表示では依然読めない**(情報構造 1→2、3点にはしていない)。

### 残っているもの(前周からの持ち越し)
- Color=2・Accessibility=2: `--ink3`のコントラスト不足(全画面共通、トークン判断待ち)

### 確認テーブルの残り2案(A案採用後も有効。次に情報構造を扱う周で再検討)
ユーザーに提示した3案のうちAを採用した。BとCは表の意味・情報構造そのものを変えるため見送ったが、
「更新後の値が初期表示で読めない」という根本は残っている。前提が変わったら再検討する。
- **B案: 更新前/更新後の縦積み** — 横スクロールが消える。行の高さが倍になり、貼り付け後3167px=3.8画面がさらに伸びる
- **C案: 変更のある行だけ更新前を出す** — 高さも横幅も減る(3周目の実測で20行中17行が「変更なし」)。表の意味が変わる

## 「調べる項目を選ぶ」の未解決事項(2026-08-13、ユーザー報告のアドホック修正)

正式な12次元レビューは未実施(銘柄詳細画面全体は「要レビュー」のまま)。詳細は`DECISIONS.md`の同日エントリを参照。

### 解消済み
1. ~~「深掘りテーマ」欄が銘柄詳細の「注目テーマ」カードと保存先(`DB.askPrefs.themes`)を共有しており、入力してボタンを押すと永続設定の「注目テーマ」まで上書きされ、かつ画面を開き直しても前回の入力が残り続けていた~~ → 保存先を`STATE._deepTheme`(一時・非永続)に分離し、画面を外部から開くたびに空欄にリセットするよう変更(ユーザー指摘・確認の上、2026-08-13)
2. ~~「調べる項目を選ぶ」から「分析セクションの管理」への遷移ボタンが、分析の編集リンクと並ぶ重複導線になっていた~~ → ボタンを削除。到達経路を分析の編集リンク1つに統一(ユーザー指示、2026-08-13)

## 指標マスターの表記ゆれ統合(2026-08-13、ユーザー質問がきっかけのアドホック修正)

正式な12次元レビューは未実施(指標編集シート・業績推移編集シートは「要レビュー」のまま)。詳細は`DECISIONS.md`の同日エントリを参照。ユーザーの質問「業績推移のデフォルト指標にEPSはある?プロンプトは1株あたりの純利益になってるけど整合取れてるかな」から調査が始まり、説明書(briefingText)の指標一覧に重複した名前が載り続け、表記ゆれが自己増殖する構造上の問題が見つかった。

### 解消済み
1. ~~EPS↔1株当たり当期純利益、ROE↔自己資本利益率など、英語略称と日本語表記が別々の指標として登録される表記ゆれが、既存の類似度判定(2文字組一致率)では検知できなかった~~ → 既知のエイリアス表(EPS/ROE/PER(予想)/PBR/FCFの5指標)を`nearestMetric`に追加(ユーザー指示、2026-08-13)
2. ~~ROE(指標)と自己資本利益率(業績推移)、営業利益率(指標)と売上高営業利益率(業績推移)が、区分違いの重複として存在していた~~ → ROEは業績推移側の既定に統一(指標マスターからは非表示・削除はしない)。既存データも同様に統合するマイグレーションを追加(ユーザー確認の上、2026-08-13)

### 見送った項目
- **表記モード設定(英語略称⇔日本語表記の切り替えUI)**: 当初ユーザーから提案があったが、「英語略称だけでいいや」という判断で不要になった。設定UIは作らず、英語略称を正としてエイリアス表で検知するだけにした。

## 指標設定シートのトグル化(2026-08-15、ユーザー指示によるアドホック修正)

正式な12次元レビューは未実施(指標編集シート・業績推移編集シートは「要レビュー」のまま)。詳細は`DECISIONS.md`の同日エントリを参照。「実装前にサンプル画面を見せて」という明示指示があったため、index.htmlを編集する前に実ブラウザでBefore/Afterのスクリーンショットを作成しユーザー承認を得てから実装した。

### 解消済み
1. ~~表示On/Off・グラフOn/Offがボタン/バッジ混在で、見た目の統一が無かった~~ → 既存の`.sw`トグルスイッチ部品(メモ候補選択で使用中)に統一(ユーザー指示、2026-08-15)
2. ~~業績推移のグラフOn/Offボタンが行の左端、名前より先に来ていた~~ → 右端の表示スイッチの隣へ移動(ユーザー指示、2026-08-15)
3. ~~業績推移テーブルで指標名が長いと1行のまま伸び、年度の数値列が右へ押し出されて見えなくなっていた(実測: 名前列250px)~~ → 名前列を125px固定・2行表示に変更(ユーザー指示、2026-08-15)
4. ~~業績推移テーブルの「表のみ」チップが行内の要素を増やしていた~~ → 削除。グラフに出ていないことを示す文字色を落とす表現は残した(ユーザー指示、2026-08-15)

### 解消済み(2026-08-15追記: グラフ選択方式の変更)
5. ~~グラフに出す指標を、設定画面で事前に(最大4件まで)選んでおく方式だった~~ → 銘柄詳細の業績推移テーブルの行をタップした指標が、その場でグラフになる単一選択・上限なしの方式に変更。設定画面のグラフOn/Offトグル・上限4件(`GRAPH_MAX`)は廃止(ユーザー指示、2026-08-15。詳細は`DECISIONS.md`の同日2件目のエントリ)

### 残っているもの
- **旧データに残る`mm.trend[].graph`キー**: グラフのOn/Off概念が無くなったため読まれなくなったが、削除の移行はしていない(害の無い残置キー)。次に指標マスターの構造を触る際にまとめて掃除する。

## 全プロンプトとアプリ内定義の整合(2026-08-13、ユーザー依頼による網羅監査)

外部AIへ渡す全プロンプト(説明書・調査の質問文・リマインダー設計・表記そろえ・整形しなおし・リマインド時の確認)を、アプリ側の定義(指標マスター4種・分析セクションマスター4種・`CATS`・`FREQS`・`parseMd`・`resolveSection`・`parseReminder`)と突き合わせた。詳細は`DECISIONS.md`の同日エントリを参照。

**結果: 動的生成している部分はすべて整合。問題は固定文に集中していた。**

### 解消済み
1. ~~説明書の✕例に、既定から外れた`営業利益率`が残り、存在しない指標を引き合いに出していた(PR #136の副作用)~~ → ✕例を`METRIC_ALIASES`と実在するマスターから動的生成に変更(2026-08-13)
2. ~~アプリが検知できる別名(`EPS↔1株当たり当期純利益`等)を、外部AIには一切伝えていなかった(事後警告のみで予防していなかった)~~ → 説明書で名指しの禁止リストとして提示(2026-08-13)
3. ~~プロンプトとアプリの受け入れ定義の整合が無検査だった~~ → `test/e2e/promptsync.js`(29項目)を追加。往復検査(プロンプトが指示した書式の回答を実際にパーサーへ通す)まで含む(2026-08-13)

### 監査で問題なしと確認できたもの(回帰テストで固定済み)
- 説明書の指標一覧4種・業績推移がマスターと完全一致
- 説明書が名指しする分析項目73件すべてが`resolveSection`で解決でき「その他」に落ちない
- 説明書どおりの回答が、読めなかった行0件・ブロック誤配0件・「新しい指標」警告0件で取り込める
- `genOrderNew`の指標名がマスターと完全一致
- `copyRegPrompt`のカテゴリ一覧=`CATS`、頻度一覧が`FREQS`を網羅、出力フォーマットが`parseReminder`で完全に往復でき次回発火まで計算できる

### 残っているもの
- **`FCF`のエイリアスが休眠状態**: `FCF`はどのマスターにも無いため、説明書にも載らず取り込み前の警告も出ない(比較対象の一覧に無いため)。`FCF`を業績推移の既定に加えれば両方とも自動で有効になる。現状は仕様として`promptsync.js`で固定済み(「マスターに無い指標の別名は説明書に書かない」)。

## 保留課題

### 配色の見直し
`--navy:#123a5e` が重い。タブバー・バッジ・グラフ・チップに波及するため、実装前に使用箇所の洗い出しと案の提示が必要。

**状態:** 保留
**境界:** トークン変更＝ユーザー確認が必要。

### padding / margin のトークン化
現在 spacing 契約は `gap` にのみ適用されている。padding / margin はトークン外の値が正当な実装として存在する(入力欄の `padding:10px 12px` など)。統一するかどうかは未決。

**状態:** 保留
**境界:** トークン範囲の拡張＝ユーザー確認が必要。


## 完了

### 色トークン・esc/escArg・シート内タップ領域の検査(2026-08-09)
`test/e2e/colortoken.js` / `escuse.js` / `sheettap.js` を追加。無検査だった3項目を機械検査下に置いた。
シート内の実測で `.stat` バッジ36px・`select` 43pxの違反が見つかり、あわせて44pxへ修正した。
