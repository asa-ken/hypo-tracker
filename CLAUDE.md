# hypo-tracker プロジェクト規約

## 応答言語
- 応答は日本語で行う。

## コミット運用
- 作業の区切りごとに git commit する。いつでも切り戻せる状態を保つのが目的。
- git commit(git add含む)・git push・PRの作成・mainへのマージは確認なしで自動実行してよい。コードの変更が完了したら、pushしてPRを作成/更新し、mainまでマージするところまで一気に行う。

## lib/ と index.html の関係
- `lib/` 配下の各ファイルは必ず CommonJS(`module.exports`)のまま維持する。ES Modules化しない。
  - 理由: `index.html` が `<script src="lib/...">` で読み込み、`onclick="..."` から直接グローバル関数として呼ぶ構成のため、ES Modules化すると本体が壊れる。
  - テストファイル(`test/`)側は ESM の `import` を使ってよい(Vitest が CJS→ESM の相互運用を吸収する)。
- `index.html` 本体へ `lib/` を取り込む統合作業(スクリプトタグの追加、関数呼び出し箇所の書き換えなど)は、明示的に指示されたときだけ行う。`lib/` の切り出し作業だけでは本体に手を入れない。
  - これは「`lib/` を切り出したあとの本体差し替え」のことであり、UIの変更を指すものではない。UIの変更は下記の通り確認不要。

## 確認ルール(作業の自動実行範囲)
- 以下は確認なしで自動的に進めてよい:
  - `lib/`・`test/`・`index.html` を含む、すべてのファイルの作成・編集
  - 読み取り系コマンド(`npm test`、`git status`、`git diff`、`git log` など)、開発中のサーバー起動・停止(`python3 -m http.server`、`lsof`/`kill`など)
  - `git add` / `git commit` / `git push` / PRの作成・更新・mainへのマージ
- 以下は必ず事前にユーザーに確認する:
  - デザイントークン(spacing / font-size / color / 44px下限 / 2列グリッド)の定義変更
  - 情報アーキテクチャ・主要ワークフローの変更
  - `rm` を伴う削除
  - `node_modules` 以外に対するその他の破壊的操作

(上記の確認範囲は `.claude/settings.json` の permissions にも反映済み)

### デザイン変更は事前確認ではなく事後の一括確認にする
UI/UXの自律改善では、**機能そのものを改変しないデザイン変更は、事前確認なしで進め、1周が終わった時点でまとめて確認を受ける。**

**条件:** 各変更が**PR単位で独立して切り戻せる**こと。切り戻せない形になる場合はこの扱いを適用せず、事前に確認する。

この扱いを適用してよいもの(**デザイン変更**):
- 表示・配置・余白・色の適用先・文言・空状態やエラー状態の表し方
- 開閉、グルーピング、並び順の見せ方
- 既存トークン・既存パターンを、それが欠けている箇所へ適用すること
- 既に存在する画面への到達経路を足すこと(遷移先そのものは増やさない)

この扱いを適用しないもの(**機能の改変**。従来どおり事前に確認する):
- 保存・変換・削除の挙動、既存データとの互換性
- リマインダーの発火条件、スケジュール計算(`calcNextFire` / `matchFreq` など)
- 取り込みの解釈(`parseMd` など)
- 画面の増減、どの情報をどの画面に置くかの変更(＝情報アーキテクチャ)
- ユーザーの操作手順そのものの増減(＝主要ワークフロー)
- デザイントークンの**定義**変更(適用先の変更は定義変更ではない)

**迷ったら確認側に倒す。** この区分は自律の範囲を広げるためではなく、確認の回数を減らして観察と実装の速度を上げるためにある。判断がつかないものを「デザイン変更」に寄せると、この運用自体が信用を失う。

1周の終わりには、出したPRを一覧にして、変更内容・根拠・切り戻し単位をまとめて報告する。ユーザーはそこで個別に差し戻しを指示できる。

## UI/UX 自律改善
UI/UX改善を行うときは、詳細を `docs/design/` 配下の文書で確認する。
- 判断基準: `docs/design/DESIGN_PRINCIPLES.md`
- 評価方法: `docs/design/DESIGN_SCORECARD.md`
- 実行手順: `docs/design/UI_REVIEW_LOOP.md`
- デザイントークン: `docs/design/DESIGN_SYSTEM.md`
- 意思決定履歴: `docs/design/DECISIONS.md`
- 巡回対象: `docs/design/REVIEW_BACKLOG.md`
- 背景と経緯: `docs/design/BACKGROUND.md`

### Digest — Decision Hierarchy
1. 壊れる・操作不能・アクセシビリティ重大問題を最優先する。
2. 次に認知負荷、操作摩擦、情報構造の問題を解消する。
3. 次に視覚的ノイズ、一貫性、情報密度を改善する。
4. 次に細かな視覚的洗練を行う。
5. 装飾だけの変更は最後にする。
6. デザイントークンの定義そのものは自律的に変更しない。変更案はユーザー確認に回す。

### ユーザーに聞く / 聞かない
- デザイントークンの定義変更は必ず確認する。
- 情報アーキテクチャ・主要ワークフローの変更は必ず確認する。
- `rm` を伴う削除、その他の破壊的操作は必ず確認する。
- `index.html` の編集、`git push`、PR作成・マージ、サーバー起動・停止は確認不要。UIは全て `index.html` にあるため、ここで止めると自律ループが成立しない。
- 上記以外のUI改善の細部は、既存ルールとテストに適合する限り自律判断する。
- 事実・実測・テスト結果が不足して判断できない場合は、推測で実装せず確認する。

### 観察は広く、実装は狭く
「1周=1テーマ」が縛るのは**実装するテーマの数**であって、観察の範囲ではない。
- Phase 1〜2(観察・問題の列挙)は、`DESIGN_SCORECARD.md` の**12次元すべて**を対象にする。
- Phase 3(仮説の選択)以降で初めて1テーマに絞る。
- この周で直さないと決めた問題は `REVIEW_BACKLOG.md` に残す。観察して記録しなければ、次周には存在しなかったことになる。

最初に見つけた問題で観察を打ち切ると、早くPRが出せて成果に見えるぶん、誤りに気づきにくい。
2026-08-09のホームで実際に発生し、`実運用摩擦=0` の欠陥を載せたまま完了扱いにした。

### Definition of Done
- **Phase 1 と Phase 8 の12次元スコアカードが両方そろっている。**
- **0点の次元が1つも残っていない。**(残る場合は `要レビュー` のままにし、次周へ送る)
- 対象画面を実データで表示して確認した。**通常・ゼロ状態・多件数・2回目の4状態を見た。**
  多件数は `test/e2e/fixtures/realdata.json`(実運用規模)を使う。`testdata.json` を少し増やして代用しない。
- **Phase 1.5a のワークフロー通し実行を行い、「引っかかった瞬間」を一覧にした。**(0件なら0件と書く)
- **Phase 1.5b の3レンズ(記憶・再入力/反復/手作業の残存)を適用し、機会候補を審査した。**
  発見は必須だが採用は任意。採用0件でも、審査した記録(候補・効果・コスト・代替・判定)を残す。
- UI変更について、必要なDOM・画素・スクロール等の測定を行った。
- `npm test` と `npm run e2e` がともに成功した。
- 既存トークンからの逸脱がない。
- 変更理由と根拠を `docs/design/DECISIONS.md` に記録した。
- この周で直さなかった1点以下の次元を `REVIEW_BACKLOG.md` に残した。
- 1改善テーマ=1PRの単位になっている。

**テストの成功はスコアカードを書いた証拠にならない。** 機械的検査はこの手順自体を検査しない。

### Stop Conditions
- **12次元のスコアカードがそろわないまま Phase 3 へ進まない。**
- **Phase 1.5a のワークフロー通し実行をしないまま Phase 3 へ進まない。** 画面を見るだけで終えない。
- **Phase 1.5b の3レンズを適用しないまま Phase 3 へ進まない。** 候補が無くても審査記録を残す。
- **Phase 8 の再採点に0点の次元がある間は完了扱いにしない。**
- `npm test` または `npm run e2e` が失敗している間は完了扱いにしない。
- 1周に複数テーマを**実装**しない(観察は全次元で行う)。
- 同じ画面を2周連続で確認し、新規に見つかる問題が主観的なものだけなら停止する。
- トークン変更、情報アーキテクチャ変更、破壊的操作が必要になった時点で停止して確認する。
- 測定結果が改善仮説を支持しない。
